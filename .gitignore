*.pyc
output/
.venv/
*.db
*.log
.env
primary_school.db
web/node_modules/
web/dist/
admin/node_modules/
admin/dist/
tools/mysql_backup_*.json
demo/
.paper_cache/

# 临时文件目录（采集/调试脚本与日志归入 temp/，不入库）
temp/
# 运行日志目录（按天+大小滚动，不入库）
log/
# WorkBuddy 本地数据（不入库）
.workbuddy/
# 题库更新脚本：本地 generate 生成、手动传到线上执行，不进 git
qb_versions/
data/
.env.prod

# 临时盘点 / 遗留脚本（勿提交）
tools/_*.py
endpoint_baseline.txt
tools/endpoint_snapshot.py

# 定时器运行时文件（线上生成：任务状态 + 单实例锁，勿入库）
# 未忽略会导致线上 git 工作区变脏，一旦 `git add -A` 就把线上运行状态提交进仓库。
tools/.scheduler_state.json
tools/.scheduler.lock

# pre-commit 本地 venv 缓存（沙箱指向项目内可写目录，勿入库）
.pc_cache/
