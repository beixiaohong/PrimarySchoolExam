# ════════ 线上库（SQLPub / 生产）连接配置（仅用于 tools/sync_prod_to_local.py）════════
# 复制本文件为 .env.prod 并填入真实值。此文件不要提交、不要与 .env 混淆。
# 同步工具对线上库只读 SELECT，不会修改线上任何数据。

# 填写 SQLPub 提供的连接信息（参考 .env.example 里的 DB_HOST/DB_PORT，通常形如 mysql*.com / 端口 3311）
PROD_DB_HOST=mysql6.xxxx.com
PROD_DB_PORT=3311
PROD_DB_USER=sijin0813
PROD_DB_PASSWORD=
PROD_DB_NAME=schoolexam
