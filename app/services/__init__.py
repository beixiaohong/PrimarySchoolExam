"""业务服务层：业务规则、算法与外部 IO 封装，不直接处理 HTTP。

约定：
- 可被多个路由复用；routers 只做参数校验与鉴权，规则一律下沉到本层。
- 外部阻塞调用集中在本层（`ai.py` 唯一 AI 入口 / `mailer.py` / `sms.py` / `paper_crawler.py`），
  便于统一限流、计费与容错。
- 🚫 连接池铁律：严禁在持有 DB 会话时发起外部阻塞调用，否则长时间占连会导致
  QueuePool 耗尽、全站卡死。正确姿势是「分段短会话」：读写 → 关 → 调外部 → 再开短会话落库。
  详见 `docs/架构说明书.md` §6.1。
"""
