[pytest]
# 仅从 tests/ 收集用例，避免根目录下 temp/ 等遗留调试脚本（非标准 pytest 用例、
# 会触发 urllib 连本地服务或导入不存在模块）在收集阶段中断整套运行。
testpaths = tests
norecursedirs = temp .venv venv node_modules dist build
python_files = test_*.py
addopts = -q
